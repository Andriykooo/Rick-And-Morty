const constants = {
  schema: "https://",
  domain: process.env.NEXT_PUBLIC_API || "rickandmortyapi.com",
  base: "/api",
  characters: "/character",
  location: "/location",
};

export default constants;
